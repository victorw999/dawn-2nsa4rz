{% layout none %}

{%- capture media -%}
  
    {
      "handle": {{ product.handle | json }},
      "product-template" : {{ template | json }},
      "images" : {{ product.images | json }},
      "media" : {{ product.media | json }}
    }
  
{%- endcapture -%}
{ "media": {{ media }} }
