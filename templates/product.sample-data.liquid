{% layout none %}

{%- capture somedata -%}
  
    {
      "handle": {{ product.handle | json }},
      "title": {{ product.title | split: '|' | last | strip_newlines | strip | json }},
      "template" : {{ template | json }}
    }
  
{%- endcapture -%}
{
  "sample-data": {{ somedata }}
}